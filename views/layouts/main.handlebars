<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/reset.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link
      href="https://fonts.googleapis.com/css2?family=Amatic+SC&family=Montserrat&family=Over+the+Rainbow&display=swap"
      rel="stylesheet">
    <link rel="stylesheet" href="/css/jass.css" />
    <title>Sweet Solution</title>
  </head>

  <body class="d-flex h-100 text-center text-bg-dark">
    <!-- NAVIGATION BAR -->
    <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
      <header class="mb-auto">
        <div>

          <img class="logo float-md-start" width="40" height="40" src="\images/logo.png">
          <h3 class="float-md-start px-1">Sweet Solution</h3>
          <nav class="nav nav-masthead justify-content-center float-md-end">
            <a class="nav-link fw-bold py-1 px-2 active" aria-current="page" href="/">Home</a>
            <a class="nav-link fw-bold py-1 px-2" href="/category">Shop</a>


            <!-- Only show cart when they have logged in -->

            {{#if loggedIn}}
            <a id="logout" class="nav-link fw-bold py-1 px-2">Logout</a>
            <a class="nav-link fw-bold py-1 px-2" href="/order">Order</a {{else}} <a class="nav-link fw-bold py-1 px-2"
              href="/login">Login</a>
            {{/if}}
          </nav>
        </div>
      </header>

      {{{body}}}
    </div>


    {{#if loggedIn}}
    <script src="./js/logout.js"></script>
    {{/if}}
    <script src="./js/login.js"></script>
  </body>

</html>